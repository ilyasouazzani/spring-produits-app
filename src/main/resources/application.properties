# ==============================
# APPLICATION CONFIG
# ==============================
spring.application.name=spring-produits-app
server.port=8080

# ==============================
# ACTIVE PROFILE: h2 or mysql
# ==============================
spring.profiles.active=h2

# ==============================
# H2 DATABASE (DEV)
# ==============================
spring.datasource.url=jdbc:h2:mem:produits_db;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ==============================
# JPA / HIBERNATE
# ==============================
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==============================
# THYMELEAF
# ==============================
spring.thymeleaf.cache=false

# ==============================
# LOGGING
# ==============================
logging.level.ma.enset=DEBUG
logging.level.org.springframework.security=INFO

# ==============================
# MySQL CONFIG (commenté - activer pour prod)
# Changer spring.profiles.active=mysql
# ==============================
# spring.datasource.url=jdbc:mysql://localhost:3306/produits_db?createDatabaseIfNotExist=true
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=yourpassword
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.hibernate.ddl-auto=update
